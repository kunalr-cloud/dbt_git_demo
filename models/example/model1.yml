version: 2

models:
  - name: model1
    columns:
      - name: customerID
        description: "customerID"
        tests:
          - unique
          # - relationships:
          #     to: ref('model2')
          #     field: customerID
      - name: first_name
        description: "first_name"
        #tests:
         # - unique

      - name: last_name
        #tests:
          #- unique

      - name: gender
        tests:
          - accepted_values:
              arguments:
                values: ["male", "female"]